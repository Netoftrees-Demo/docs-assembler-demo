#### What does it check

It checks that images exist, that every step you call is real, and that every piece of reusable text is defined. Not after publishing. Before. You fix it in private; your readers see only the final, working version.

